{
  "name": "doodle-mobile-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check 'api/[...path].js'"
  },
  "engines": {
    "node": ">=20"
  }
}
